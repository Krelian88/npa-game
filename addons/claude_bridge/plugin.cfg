[plugin]

name="Claude Bridge"
description="Exposes a local HTTP server so Claude (via MCP) can read/write scenes, scripts, logs, and editor state in real time."
author="Claude / Anthropic"
version="1.0.0"
script="plugin.gd"
